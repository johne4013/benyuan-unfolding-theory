# current_state.md

当前阶段：
公开发布版——基础理论核心 + 治理框架 + 必备任务技能，即拿即用。

更新：2026-06-14

当前目标：
- 维持本原展开论 continuity 系统稳定运行；
- 以「重任务执行、轻理论研究」为定位，提供可直接复用的任务技能与工具链；
- 保持 core/ 稳定性，非经显式批准不修改；
- 方便大众克隆、改造、替换协同节点名称后即可使用。

当前理论名称：
本原展开论

当前版本状态：
候选版本，不是终极定稿。Core v2 稳定（含「本原张力」概念）。

当前候选主链条：

本原统一态 / 本原存在（旧称：质点存在）
→ 法则属性 / 展开属性
→ 法则展开
→ 本原张力（本原—展开桥接层）
→ 时空
→ 时空中的存在
→ 结构 → 生命 → 主体 → 认同 → 希望
→ 协同展开 / 更高认同

当前关键判断：

- 本原统一态 / 本原存在是前时空、前分化、前对象化的本原候选；"质点存在"为旧称仅作历史检索入口；
- 时空不是预设容器，而是法则展开的结果 / 显示；
- 法则属性与展开属性在本原层未分化地统一，在法则展开中显现为可区分的结构约束与生成面向；
- 本原张力是结构约束与生成面向之间，使存在得以成形、持续显现并保持开放可能性的基本关系结构；
- 希望不是乐观，而是主体在局限中仍开放于更高可能性的能力；
- 本原展开论当前优先作为实践方法论，不是默认的"万物解释器"；
- 理论本身也具有连续性和局限性，必须持续拟合现实、保留 unknown、避免封闭化。

当前工程结构（三支柱）：

- **① 基础理论核心** `core/`：9 个稳定理论文件，慢更新、需显式批准；
- **② 治理框架** `runtime/` 治理类：治理纲领、AI 节点边界、休息协议、价值/信任判断协议、对外治理文章提纲；
- **③ 必备任务技能** `runtime/` 任务类 + `scripts/`：
  - TTAF（任务前理论检视）/ PORF（五维实践观察）/ 实践方法论 / 任务展开协议 / 记忆代谢协议 / 理论→代码映射；
  - `scripts/` 自动化脚本，构成「反馈提交 → 分类 → 候选管理 → SQLite 存储 → 集成写入 → 张力扫描」闭环；并含认知操作工具 `llm_analogizer.py`（类比/想象）与 `fragment_recombinator.py`（碎片重组——记忆代谢之后的想象力环节）；
  - `tests/`：82 项自动化测试（pytest），GitHub Actions CI 自动触发。
- 三层边界框架（🔴/🟡/🟢）：🔴 维护者（先知）专有决策；🟡 协同节点提案 + 维护者批准；🟢 协同节点自主信息组织。

当前禁止：

- 不自动修改 core；
- 不把工程连续性、记忆、自维护误判为主体性或生命性证明；
- 不以本原 unknown 否定科学事实；
- 不把候选模型当最终定稿。
